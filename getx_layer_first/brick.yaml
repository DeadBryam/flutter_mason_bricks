name: getx_layer_first
description: Brick generator for a new feature, using the clean architecture pattern (layer first)
version: 1.0.1
environment:
  mason: ^0.1.1
vars:
  feature_name:
    type: string
  package_name:
    type: string
